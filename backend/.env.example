
# FlowSight Backend Environment Variables

# App settings
DEBUG=false

# Stub mode - set to true to return mock responses without calling watsonx
# Set to false when ready to use real watsonx Orchestrate
STUB_MODE=true

# CORS (comma-separated origins, or * for all)
CORS_ORIGINS=["http://localhost:3000", "http://localhost:5173"]

# =============================================================================
# watsonx Orchestrate Settings (Chat/Agent API)
# =============================================================================
# IBM Cloud API Key - create at https://cloud.ibm.com/iam/apikeys
WATSONX_API_KEY=your-ibm-cloud-api-key

# watsonx Orchestrate URL - check your instance region
WATSONX_URL=https://api.us-south.assistant.watson.cloud.ibm.com

# Agent ID - get from watsonx Orchestrate after creating your agent
WATSONX_AGENT_ID=your-agent-id

# Optional: Agent Environment ID
WATSONX_AGENT_ENV_ID=

# =============================================================================
# ETL Pipeline Settings (for production data ingestion)
# =============================================================================

# GitHub API Token (get from https://github.com/settings/tokens)
# Scopes needed: repo, read:org
GITHUB_TOKEN=your_github_token_here

# Astra DB Configuration (vector database for AI)
ASTRA_DB_TOKEN=your_astra_token
ASTRA_DB_ENDPOINT=https://your-db-id-region.apps.astra.datastax.com
ASTRA_DB_KEYSPACE=flowsight

# watsonx.ai Configuration (for embeddings in ETL pipeline)
WATSONX_AI_API_KEY=your_watsonx_ai_api_key
WATSONX_AI_PROJECT_ID=your_watsonx_project_id
WATSONX_AI_URL=https://us-south.ml.cloud.ibm.com

# Slack Configuration
SLACK_TOKEN=xoxb-your-slack-bot-token

# Jira Configuration
JIRA_URL=https://your-domain.atlassian.net
JIRA_EMAIL=your-email@company.com
JIRA_API_TOKEN=your_jira_api_token

# Microsoft Teams Configuration (Graph API)
TEAMS_TOKEN=your_microsoft_graph_access_token
